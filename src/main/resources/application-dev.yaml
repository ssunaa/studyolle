spring:

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
      url: jdbc:postgresql://localhost:5432/testdb
      username: testuser
      password: testpass

  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true